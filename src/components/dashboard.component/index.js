import React, { useState } from 'react';
import connectStateProps from '../../utils/state.connect';

const Dashboard=()=> {
    return (
        <>Dashboard</>
    )
}

export default connectStateProps(Dashboard);

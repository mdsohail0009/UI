import { Component } from "react";

class BankDetails extends Component{

    render(){
        const {addressType,transferType,onSubmit,bankDetails={}}=this.props;
        return ""
    }
}
export default BankDetails;
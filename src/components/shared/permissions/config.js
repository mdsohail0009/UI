const KEY_URL_MAP = {
    "/payments": "billpayments",
    "/cards": "cards",
    "/addressBook": "addressbook",
    "/documents": "documents",
    "/cockpit": "cockpit",
    "/": "cockpit"
}
export { KEY_URL_MAP }